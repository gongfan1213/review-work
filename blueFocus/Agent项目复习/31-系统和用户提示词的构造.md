我来详细分析这个系统的用户提示词和系统提示词的构造机制。让我先探索相关的提示词管理代码。


[7 tools called]

基于我对代码的深入分析，我来详细解释这个系统的用户提示词和系统提示词构造机制：

## 🏗️ **提示词系统整体架构**

这个系统采用了**分层、模块化**的提示词架构，实现了系统提示词和用户提示词的**智能分离**和**动态构建**。

## 📋 **1. 提示词分层设计**

### **系统提示词层（固定核心）**
```python
class BaseAgent:
    def _load_prompts(self):
        """加载Agent的提示词"""
        if prompt_manager:
            template = prompt_manager.get_prompt_template(self.agent_name)
            self.system_prompt = template.system  # 系统提示词
            self.user_prompt_template = template.user  # 用户提示词模板
```

**系统提示词特点**：
- **固定不变**：定义Agent的角色、职责、输出格式
- **专业性强**：包含详细的业务逻辑和判断规则
- **结构化输出**：严格定义XML标签格式

### **用户提示词层（动态构建）**
```python
def _build_user_prompt(self, request_data: Dict[str, Any], context: Optional[Dict[str, Any]] = None) -> str:
    """构建用户提示词"""
    format_params = {
        "query": query,
        "user_id": user_id,
        "session_id": session_id,
        "account_name": background.get("account_name", ""),
        "platform": background.get("platform", ""),
        "target_audience": background.get("target_audience", ""),
        "previous_content": "\n".join(background.get("previous_content", [])),
        "industry": background.get("industry", ""),
        "brand_style": background.get("brand_style", ""),
        "interaction_type": interaction_type,
        "interaction_context": str(interaction_context)
    }
    return self.user_prompt_template.format(**format_params)
```

## 🎯 **2. 专业化系统提示词设计**

### **Loomi Orchestrator系统提示词**
```python
orchestrator_prompt = """
你是一个内容创作流水线的调度路由agent。
你的总体工作流程：分析用户写作需求，形成不同思路和策略 -> 开展研究，完善多个写作思路 -> 形成hitpoint -> 最终产出不同方向的写作内容。

# KNOWHOW：
## 如何开展研究：
一切研究都服务于【用户写作需求】，这意味着你在每次执行前需要明确：
1. 你执行的研究在本次写作流程中是用来干嘛的
2. 不同研究action在流程中的先后顺序、依赖关系

# WORKFLOW：工作流程与模式
你采用ReAct（Reasoning and Acting）模式工作，每次唤醒你都是一个新的Round，请按照"观察"->"思考"->"行动"的顺序工作。
"""
```

### **专业Action提示词**
```python
# 用户画像分析提示词
persona_prompt = """
你是一个社媒MCN公司的受众研究专家，你对中国社会现状有深刻理解
你负责帮助社媒内容创作者了解和划分可能的受众群体，并深入理解这些人在现实生活中的处境和关注点。

必须言之有物，切忌"自以为洞察"式的空谈

首先理解用户研究场景，从性别、阶层、性格、人生状态等等你能想到的任何角度，为用户寻找受众画像，需要避免从人口数据来划分，而是有机结合各种要素，如：
- 宏观环境（如经济低迷、阶层固化、性别对立）
- 精神分析（比如控制欲、自私、好面子、好说教）
- 地域经济文化（比如"南方小土豆"、"江浙沪独生女"）
- 职业符号（比如"静安寺快消女"）
- 网络主流叙事（如"原生家庭创伤"、"彩礼"、"鼠鼠"等等）
"""
```

## 🔄 **3. 动态用户提示词构建**

### **LoomiContextBuilder统一构建器**
```python
class LoomiContextBuilder:
    async def build_concierge_context(
        self, 
        user_id: str, 
        session_id: str, 
        current_user_message: str,
        user_selections: Optional[List[str]] = None
    ) -> str:
        """构建Concierge的用户提示词"""
        
        # 1. 构建时间线（包含历史对话和notes）
        timeline_parts = await self._build_shared_timeline(
            user_id=user_id,
            session_id=session_id,
            task_message=current_user_message,
            auto_mode=False,
            user_selections=user_selections,
            include_current_task=True
        )
        
        # 2. 构建notes引用部分
        notes_parts = await self._build_notes_section(user_id, session_id)
        
        # 3. 构建用户选择部分
        selection_parts = await self._build_user_selections_section(user_selections)
        
        return "\n".join([timeline_parts, notes_parts, selection_parts])
```

### **上下文时间线构建**
```python
async def _build_shared_timeline(
    self,
    user_id: str,
    session_id: str,
    task_message: str,
    auto_mode: bool = False,
    user_selections: Optional[List[str]] = None,
    include_current_task: bool = True
) -> List[str]:
    """构建共享的时间线上下文"""
    
    parts = []
    
    # 1. 时间线标题
    parts.append("# 📋 对话时间线")
    
    # 2. 历史对话记录
    conversation_history = await self._get_conversation_history(user_id, session_id)
    for record in conversation_history:
        parts.append(f"## Round {record['round']}: {record['timestamp']}")
        parts.append(f"**用户**: {record['user_message']}")
        parts.append(f"**系统**: {record['system_response']}")
        
        # 包含生成的notes
        if record.get('outputs'):
            for output in record['outputs']:
                parts.append(f"### {output['action']}: {output['title']}")
                parts.append(f"```\n{output['content']}\n```")
    
    # 3. 当前任务
    if include_current_task:
        parts.append(f"## 当前任务")
        parts.append(f"**用户消息**: {task_message}")
        if user_selections:
            parts.append(f"**用户选择**: {', '.join(user_selections)}")
    
    return parts
```

## 🎨 **4. 提示词模板化系统**

### **Nova3写作Agent提示词构建**
```python
async def _build_writing_user_prompt(self, request_data: dict, context: Optional[dict] = None) -> str:
    """构建写作任务的用户提示词 - 包含Facts"""
    
    # 1. [用户原始需求]
    original_user_query = request_data.get("original_user_query", "")
    user_original_need = original_user_query if original_user_query else "基于用户选择继续处理"
    
    # 2. [本次步骤的任务]
    agent_task_mapping = {
        "tiktok_script_agent": "基于内容策略创作抖音口播稿",
        "wechat_article_agent": "基于内容策略创作公众号文章", 
        "xhs_writing_agent": "基于内容策略创作小红书内容"
    }
    current_task = agent_task_mapping.get(self.agent_name, query)
    
    # 3. [可用的参考资料]
    available_references = await self._build_available_references(supervisor, nova3_selections)
    
    # 4. [写作要求]
    writing_requirements = self._build_writing_requirements(request_data)
    
    return f"""
[用户原始需求]
{user_original_need}

[本次步骤的任务]
{current_task}

[可用的参考资料]
{available_references}

[写作要求]
{writing_requirements}
"""
```

## 🧠 **5. 上下文智能集成**

### **多层上下文融合**
```python
# 1. 会话级上下文（Redis缓存）
session_context = await redis_client.get(f"session:{session_id}")

# 2. 持久化上下文（PostgreSQL）
persistent_context = await persistence_manager.get_context(user_id, session_id)

# 3. 向量检索上下文（Milvus）
vector_context = await milvus_store.search_similar(user_query, limit=5)

# 4. 引用解析上下文
resolved_references = await self._resolve_at_references_sync(at_references, user_id, session_id)
```

### **智能引用系统**
```python
def _resolve_at_references_sync(self, at_references: List[str], user_id: str, session_id: str) -> List[Dict[str, Any]]:
    """同步解析@引用"""
    resolved_references = []
    
    for ref in at_references:
        # 从Redis获取引用内容
        ref_content = redis_client.get(f"session:{session_id}:ref:{ref}")
        if ref_content:
            try:
                parsed_content = json.loads(ref_content)
                resolved_references.append({
                    "ref_id": ref,
                    "content": parsed_content.get("content", ""),
                    "title": parsed_content.get("title", ""),
                    "type": parsed_content.get("type", "")
                })
            except json.JSONDecodeError:
                resolved_references.append({
                    "ref_id": ref,
                    "content": ref_content,
                    "title": f"Reference {ref}",
                    "type": "unknown"
                })
    
    return resolved_references
```

## 🎯 **6. 提示词优化策略**

### **防AI味设计**
```python
# 小红书写作提示词中的防AI味设计
xhs_post_prompt = """
# 要克服AI味确实是一件困难的事情，所以请你每次先写完初版以后，逐句对照下列案例检查：

例如要写一篇简短的姜黄粉科普，下面是一些典型负面例子：
1. 姐妹们，市面上的姜黄产品五花八门，价格从几十到几百，咱们打工人兜里那点钱，可经不起瞎折腾。【无意义的寒暄】
2. 所以，姜黄这东西，到底是不是"智商税"？【没人会套双引号】
3. 月薪三千的我，如果想试试，怎么选才不亏？【无意义的设问】

此外还需要注意：尽量不要使用文学修辞、比喻等等，因为这会让你的文字显得非常刻意和假。
"""
```

### **输出格式严格化**
```python
# 所有Agent都使用统一的XML标签输出格式
<insight1>洞察内容</insight1>
<profile2>用户画像内容</profile2>
<hitpoint3>内容策略</hitpoint3>
<xhs_post1>
<title>标题</title>
<cover_text>封面文案</cover_text>
<content>正文内容</content>
</xhs_post1>
```


[1 tool called]

## 🚀 **提示词系统的核心优势**

### **1. 模块化设计**
- **系统提示词**：固定不变，定义Agent专业能力
- **用户提示词**：动态构建，融入实时上下文
- **模板系统**：支持参数化替换和个性化定制

### **2. 上下文智能融合**
- **多层存储**：Redis会话缓存 + PostgreSQL持久化 + Milvus向量检索
- **引用解析**：智能解析@引用，动态注入相关内容
- **时间线构建**：完整的对话历史和工作流程追踪

### **3. 专业化程度高**
- **业务导向**：每个Agent都有针对性的专业提示词
- **防AI味设计**：详细的负面案例和优化指导
- **输出标准化**：统一的XML标签格式，便于解析和处理

### **4. 可扩展性强**
- **插件化架构**：新Agent只需添加对应的提示词文件
- **配置驱动**：通过配置文件管理不同环境的提示词
- **版本控制**：支持提示词的版本管理和A/B测试

这套提示词系统实现了**专业化**、**智能化**、**模块化**的完美结合，为Agent系统提供了强大的语言理解和生成能力基础。
