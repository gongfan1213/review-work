原因
- “unhashable type: 'slice'”表示把切片对象当成了字典键使用。常见于把非列表对象用切片语法访问，例如对 dict 做 `obj[1:3]`。
- 你的后端里 `` 会对 `content_angles` 做切片 `content_angles[1:3]`。当上游 LLM 返回的 `content_angles` 偶尔不是 list（例如是 dict 或 str）时，这个切片就会报错。

处理
- 我已把 `content_angles` 正常化为列表（dict→values 列表，str→单元素列表），并且只在类型为 list 时才做切片，已重启服务。

你现在只需在前端重试；若还有类似报错，发我对应时间点，我会对其他类似字段做同样的类型兜底。
